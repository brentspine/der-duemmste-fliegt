<style>
    .snackbar {
        position: fixed;
        width: 100vw;
        z-index: 10000;
    }

    .toast-container {
        display: flex;
        justify-content: center;
        width: 100vw;
        position: fixed!important;
    }

    .toast {
        min-width: 350px;
        background-color: #333;
        border-radius: 2px;
        width: auto;
    }

    /*.toast-close::before {
        content: attr(data-content);
        padding: attr(data-padding);
    }*/

    .toast-close {
        background-color: var(--data-background);
        padding: 10px 0px;
        border-radius: 2px;
        font-size: 1.05rem;
        height: 100%;
        overflow: visible!important; /* KEEP THIS */
        text-align: center;
    }
    .toast-close.closable {
        cursor: pointer;
    }

    .toast-close span {
        opacity: 0;
    }

    .toast-content {
        padding: 10px 10px 10px 2px;
        border-radius: 2px;
        font-size: 0.95rem;
        height: 100%;
        margin-left: 7px;
        font-family: "Times New Roman", serif;
    }

    .toast-close,
    .toast-content {
        display: inline-block;
        color: #D0D2D6;
        float: left;
    }

    * {
        box-sizing: border-box;
    }
</style>

<div class="snackbar" id="snackbar"></div>

<script src="{{ asset('/scripts/toasts.js') }}"></script>

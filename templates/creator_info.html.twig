<div id="img">
    <img src="{{ (creator.img|length > 0) ? asset(creator.img) : asset('/images/standard_profile.svg') }}" alt="{{ creator.name }}">
</div>
<span id="name" class="text">{{ creator.name|capitalize }}</span>